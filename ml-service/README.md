# Smart Energy ML Service

Endpoints: `/health`, `/train`, `/predict/latest`, `/metrics`, `/docs`.
Manual dataset labels override Arduino status-derived labels.
Models are written to `models/`; Firebase outputs go to `smartGrid/ml/latest` and `smartGrid/ml/training`.
